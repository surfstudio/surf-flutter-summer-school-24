import 'package:flutter/material.dart';

class PhotoNumber extends StatelessWidget {
  const PhotoNumber({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('13/37');
  }
}
